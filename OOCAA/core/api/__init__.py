"""API package skeleton for the `core` app.

This package contains serializers, viewsets and URL wiring for the
core application's REST API. Files are intentionally minimal — add
implementation as needed.
"""
